# Assignment Project

Uses react, vite, shadcn, tailwind
Zustand for client side state management
pnpm as package manager
eslint + prettier for linting and formatting, and husky for pre commit hook lint check
